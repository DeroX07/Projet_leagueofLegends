@model IEnumerable<Projet_LeagueofLegends.Models.Resultat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.equipe1.TeamName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equipe2.TeamName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.victoire)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.equipe1.TeamName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equipe2.TeamName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.victoire)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ResultatID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ResultatID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ResultatID })
        </td>
    </tr>
}

</table>
